# ── API Runtime Requirements ONLY ────────────────────────────────────────────
# These are the ONLY packages main.py actually imports at runtime.
# Do NOT add training/data-collection deps here (torch with CUDA, mlflow,
# dvc, airflow, transformers, snscrape, yfinance, etc.) — they bloat the
# image by 5GB+ and caused the nvidia_cudnn timeout.

# Web framework
fastapi==0.115.0
uvicorn==0.30.6

# Data
pandas==2.2.3
numpy==2.1.3

# Validation
pydantic==2.5.0

# PyTorch — CPU only (no CUDA = ~200MB instead of 530MB + 366MB nvidia_cudnn)
# Installed separately in Dockerfile via --index-url
torch==2.4.0+cpu
